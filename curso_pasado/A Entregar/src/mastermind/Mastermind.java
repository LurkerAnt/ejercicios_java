package mastermind;

public class Mastermind {

	final String colores = "0123";
	private String combinacion="230";// sacar numero de posicion y utilizar el charat(posicion) para poder usarlo con
										// numeros y letras
	// realizar comprobaciones con las posiciones
	/*
	 * propiedad para poner la longitud y para la aleatoriedad necesitamos una
	 * propiedad que sea un array que sea las combinaciones propuestas array this
	 * <-- eso hay que usar e investigar. resultados se deben almacenar tambien, con
	 * un array
	 */
	230// combinacion
	123// a comprobar-->sacar :00 (2 aciertos, ninguno en posicion
	111 023// 000
	032// 010 error de programacion hay que concatenar los 1 primero para no dar pistas
		// 100
		// las comparaciones hay que filtrarlas si ya han coincididio se deben saltar
		// las comprobaciones
		// hay que hacer un contador de jugadas
		// getters y setters
		// get combinacion
		// get combinacion comprobada 1

}
